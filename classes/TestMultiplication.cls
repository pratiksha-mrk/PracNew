@isTest
public class TestMultiplication {
@isTest
    public static void forPositiveNumber()
    {
        integer p = CheckMultiplication.getMul(4, 4);
        system.assertEquals(16, p, 'problem in code');
    }
    
    
}