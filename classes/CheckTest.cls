@isTest
public class CheckTest {
	@isTest
    public static void forPositiveNumber()
    {
        integer p =CheckSum.getSum(78, 5);
        system.assertEquals(83, p, 'problem in code');
    }
    
    
}